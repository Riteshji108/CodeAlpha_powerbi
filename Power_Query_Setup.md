# Power Query Setup

Load each CSV with **Home > Get Data > Text/CSV**.

Recommended typing/cleanup:
- `Date`, `HireDate`, `ExitDate`, `OpenDate` => Date
- money fields => Decimal Number / Fixed Decimal
- percentage/index measures => Decimal Number
- city, department, property type, employee/property IDs => Text
- Create a Date table for Task 1 trend reporting.
